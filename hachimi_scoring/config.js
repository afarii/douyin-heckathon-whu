export const DEFAULT_WEIGHTS = Object.freeze({
  dbScore: 0.4,
  freqScore: 0.25,
  durationScore: 0.2,
  chaosScore: 0.15,
});

export const DEFAULT_RANGES = Object.freeze({
  scoreMin: 0,
  scoreMax: 10,
  hisLevelMin: 1,
  hisLevelMax: 9,
});

