# TODO

## 近期

* 接入真实后端 `POST /api/upload`，替换当前 Mock 评分。
* 明确音频上传限制，包括文件大小、录音时长和支持格式。
* 增加上传/录音错误提示，例如麦克风拒绝授权、文件格式不支持、接口超时。
* 增加加载状态和重复点击保护，避免检测过程中重复提交。
* 优化移动端录音体验，重点检查微信、抖音内置浏览器和 Safari。

## 中期

* 增加真实波形可视化，基于 Web Audio API 展示录音音量变化。
* 优化结果分享卡片，支持导出图片。
* 增加更多梗化评价模板，让不同分数段的反馈更丰富。
* 抽离评分等级配置，方便后端和前端共用规则。
* 增加基本端到端测试，覆盖上传、录音失败、Mock 回退和结果展示。

## 后期

* 接入音频特征提取与相似度算法，例如 MFCC、声学指纹或嵌入向量比对。
* 增加排行榜或匿名历史记录，提升小游戏传播性。
* 支持 PWA 安装和离线打开。
* 增加短视频片段或动态海报导出能力。
* 将静态 Demo 迁移到 Vite/React/TypeScript，方便继续扩展组件和状态管理。
