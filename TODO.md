# TODO

## 近期

* 明确前端上传限制：文件大小、最长检测时长、支持格式、失败提示文案。
* 优化移动端录音体验，重点检查微信、QQ、抖音内置浏览器和 Safari。
* 增加后端接口超时处理，让“后端校验失败”的提示更具体。
* 增加真实哈气素材回归样本，覆盖高分、中分、低分、说话声、英语听力、音乐和环境噪声。
* 调整 `voicedRatio`、气声颗粒、节奏轮廓等阈值，减少连续人声误判，同时避免误伤真实哈气。
* 将评分档位、颜色、结算素材映射抽成统一配置，避免前后端规则分散。

## 中期

* 增加真实录音波形或能量动画，只用于录音反馈，不再作为评分拆解进度条。
* 优化分享图排版，补充二维码或项目链接，方便微信/QQ 手动传播。
* 丰富评分理由模板，让不同分数段、不同扣分原因的反馈更稳定且不机械。
* 增加本机历史详情页，支持查看单次记录的分数、称号、时间和评分理由。
* 增加端到端测试，覆盖录音/上传、后端失败、结算页、分享图生成和本机排行榜。
* 为后端增加上传文件大小限制和临时文件清理策略。

## 后期

* 引入更专业的音频特征：MFCC、谱质心、谱通量、音频嵌入向量等。
* 建立小型标注集，用真实哈气、说话、音乐、噪声样本评估阈值准确率。
* 考虑将评分服务拆成独立 API，方便部署和压测。
* 支持 PWA 安装和离线打开，但评分仍需后端校验。
* 增加短视频或动态海报导出能力，提高展示和传播效果。
* 迁移到 Vite/React/TypeScript，便于继续扩展组件、状态和测试体系。

## 已完成

* 接入 `POST /api/upload` 后端评分接口。
* 移除前端 Mock 打分兜底，检测失败时不生成假分数。
* 使用 `基米素材/haqi.mp3` 作为前端示例和提交时参考素材。
* 增加结算页面、分数圆环、称号、评分理由和结算动图。
* 增加分享图生成、保存、系统分享、QQ/微博入口和微信文案复制路径。
* 增加本机排行榜和清空功能。
* 增加连续语音/英语听力式音频误判限制。
* 增加后端算法测试和 `/api/upload` 接口测试。
